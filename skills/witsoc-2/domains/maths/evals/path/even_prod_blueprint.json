{
 "metadata": {
  "name": "wit_even_prod",
  "kind": "THEOREM"
 },
 "target_formalization": {
  "claim": "for every natural number n, the product n times n plus one is divisible by two",
  "hypotheses": [],
  "formal_statement": "theorem wit_even_prod (n : ℕ) : 2 ∣ n * (n + 1)",
  "preamble": "import Mathlib"
 },
 "target_protection": {
  "statement_tampering_forbidden": true,
  "frozen_target_sha256": "e09be7b0fd470bdb4af728e7055d7abaf8d67ac8b687bd8823afb66f6d5418fd",
  "original_statement": "for every natural number n, the product n times n plus one is divisible by two",
  "authorized_mutations": []
 },
 "external_dependencies": [],
 "lemma_plan": [
  {
   "step_id": "1",
   "type": "HAVE",
   "statement": "if two divides k times k plus one then two divides k plus one times k plus two",
   "method": "expanding the product and peeling off a multiple of two",
   "formalization": {
    "type": "∀ k : ℕ, 2 ∣ k * (k + 1) → 2 ∣ (k + 1) * (k + 1 + 1)",
    "tactic": "intro k ih\nobtain ⟨m, hm⟩ := ih\nexact ⟨m + k + 1, by rw [show (k+1) * (k+1+1) = k * (k+1) + 2*(k+1) by ring, hm]; ring⟩"
   }
  },
  {
   "step_id": "2",
   "type": "SHOW",
   "statement": "for every natural number n, the product n times n plus one is divisible by two",
   "depends_on": [
    "1"
   ],
   "method": "induction on n, base by evaluation and step by the lemma above",
   "formalization": {
    "type": "2 ∣ n * (n + 1)",
    "tactic": "induction n with\n| zero => simp\n| succ k ih => exact step1 k ih"
   }
  }
 ],
 "generator_directive": {
  "status_to_assert": "UNVERIFIED"
 },
 "_next": [
  "fill every FILL-ME; a step with no formalization keeps its hole and the artifact is honestly incomplete rather than confidently wrong",
  "declare every result you intend to cite in external_dependencies — a citation outside it is refused, because out of scope is not a step",
  "python3 scripts/premise_preflight.py --blueprint <this file>",
  "python3 scripts/produce.py --blueprint <this file> --tier kernel"
 ]
}