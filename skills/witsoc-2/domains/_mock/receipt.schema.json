{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "_mock-receipt-v1",
  "title": "Mock domain receipt",
  "description": "This fixture adds nothing to frame-receipt-v1 — the frame's receipt is already sufficient for a deterministic checker. A real pack extends it with whatever its backend produces: solver logs, run manifests, instrument calibration snapshots.",
  "allOf": [{ "$ref": "frame-receipt-v1" }],
  "type": "object",
  "properties": {
    "normalization_applied": {
      "description": "Which normalization the checker actually used. Recorded so a mismatch against the frozen claim is detectable after the fact.",
      "enum": ["strip_whitespace", "exact_bytes"]
    }
  }
}
